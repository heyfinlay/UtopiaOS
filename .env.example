VITE_API_URL=http://localhost:8787
PORT=8787
WEB_ORIGIN=http://localhost:5173

# The API switches to Supabase persistence only when all three values below exist.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
UTOPIA_OWNER_ID=

# Optional. If provided, the API will pipe the research prompt into this command.
# Example: OPENCLAW_COMMAND="openclaw run --json"
OPENCLAW_COMMAND=
