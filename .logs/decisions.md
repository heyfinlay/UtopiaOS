# Decision Log

## 2026-04-30

- Use Hono instead of Fastify for a lighter API bridge and easier composition with Zod validation.
- Start with a controlled OpenClaw gateway abstraction so the first version can run with a deterministic mock when the local CLI is unavailable.

